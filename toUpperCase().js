var btn = document.querySelector('#btn');
var line = document.querySelector('#line');
var lineValue = line.value;
var arr = lineValue.split();

function run () {
  for (let i = 0; i < arr.length; i++) {
    var colors[0] = arr[0].toUpperCase();
   }
   console.log(colors);
 }
